- provide(:title, 'Profile')

.float-right= link_to 'Edit profile', edit_user_path(@user), class: 'btn btn-primary'

%h1.display-4= @user.to_s

- if @user.admin?
  %p Administrator
%p= @user.organization
%p= @user.email
%p= "#{@user.city}, #{@user.state} #{@user.zip}"
%p= @user.phone
